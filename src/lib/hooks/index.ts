// =============================================================================
// Nexus Journal — Hooks Barrel Export
// =============================================================================

/**
 * Re-exports all custom hooks for convenient single-path imports:
 *
 * ```ts
 * import { useTheme, useScrollProgress, useMediaQuery, useKeyboardShortcut } from '@/lib/hooks';
 * ```
 */

export { useTheme } from './useTheme';
export { useScrollProgress } from './useScrollProgress';
export { useMediaQuery } from './useMediaQuery';
export { useKeyboardShortcut } from './useKeyboardShortcut';
